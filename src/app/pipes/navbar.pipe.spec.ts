/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { NavbarPipe } from './navbar.pipe';

describe('NavbarPipe', () => {
  it('create an instance', () => {
    const pipe = new NavbarPipe();
    expect(pipe).toBeTruthy();
  });
});
